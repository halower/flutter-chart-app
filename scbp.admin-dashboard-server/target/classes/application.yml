spring:
  application:
    name: scbp-admin-dashboard-server

server:
  port: 8000

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
# 高可用支持
#   serviceUrl:
#   defaultZone: http://localhost:8001/eureka
  server:
    waitTimeInMsWhenSyncEmpty: 0
  dashboard:
    enabled: false

management:
  security:
    enabled: false